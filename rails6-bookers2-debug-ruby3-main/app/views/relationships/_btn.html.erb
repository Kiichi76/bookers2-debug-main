<% if current_user != user %>
    <%if current_user.follower_user?(user) %>
        <%= link_to "フォロー外す", user_relationships_path(user_id), method: :delete, class: options[:class].presence || "btn btn-info" %>
    <% else %>
        <%= link_to "フォローする", user_relationships_path(user_id),method: :post, class: options[:class].presence || "btn btn-info" %>
    <% end>
<% end %>